[tox]
envlist = py37, py38, py39, pypy

[testenv]
deps = 
    -rrequirements.txt
    pytest
commands = pytest

[testenv:mypy]
deps =
    -rrequirements.txt
    mypy
commands = {envpython} -m mypy src
